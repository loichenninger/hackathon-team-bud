apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pearl.fullname" . }}-test-connection"
  labels:
    {{- include "pearl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pearl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "survey-designer.fullname" . }}-test-connection"
  labels:
    {{- include "survey-designer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "survey-designer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

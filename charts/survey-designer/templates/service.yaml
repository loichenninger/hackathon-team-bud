apiVersion: v1
kind: Service
metadata:
  name: {{ include "survey-designer.fullname" . }}
  labels:
    {{- include "survey-designer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "survey-designer.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "eno-ws.fullname" . }}
  labels:
    {{- include "eno-ws.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "eno-ws.selectorLabels" . | nindent 4 }}

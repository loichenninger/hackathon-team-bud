apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eno-ws.fullname" . }}-test-connection"
  labels:
    {{- include "eno-ws.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eno-ws.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

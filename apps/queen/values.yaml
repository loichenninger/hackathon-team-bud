queen:
  ui:
    replicaCount: 1
    image:
      name: inseefr/queen
      version: 0.10.6
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: nginx
      hosts:
        - host: queen.kub.sspcloud.fr
          paths:
            - path: /
      tls:
        - hosts:
            - queen.kub.sspcloud.fr
    env:
      QUEEN_URL: https://queen.kub.sspcloud.fr
      QUEEN_API_URL: https://queen-api.kub.sspcloud.fr
      authenticationType: NONE

  api:
    name: api
    replicaCount: 1
    image:
      name: inseefr/queen-back-office
      version: 3.5.24
    ingress:
      enabled: true
      hosts:
        - host: queen-api.kub.sspcloud.fr
          paths:
            - path: /
      tls:
        - hosts:
            - queen-api.kub.sspcloud.fr
    env:
      JAVA_OPTS: -Dlogging.config=${CATALINA_BASE}/webapps/log4j2.xml -Dspring.profiles.active=dev -Dspring.config.location=classpath:/,${CATALINA_BASE}/webapps/queenbo.properties
      #Application configuration
      FR_INSEE_QUEEN_APPLICATION_MODE: noauth
      FR_INSEE_QUEEN_APPLICATION_CROSORIGIN: "*"
      FR_INSEE_QUEEN_PILOTAGE_INTEGRATION_OVERRIDE: true

  postgresql:
    enable: true
    fullnameOverride: queen-db
    auth:
      username: queen
      password: queen
      database: queen-db
      postgresPassword: postgresPassword
